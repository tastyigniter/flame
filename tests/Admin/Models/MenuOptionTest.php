<?php

namespace Tests\Admin\Models;

it('creates a new record')->skip();
it('retrieves an existing record')->skip();
it('updates an existing record')->skip();
it('deletes an existing record')->skip();
it('creates options values with new record')->skip();
it('updates options values on existing record')->skip();
it('deletes options values on existing record')->skip();
it('attach an existing record to a menu item')->skip();
it('updates options values on related menu item')->skip();
